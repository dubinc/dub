NODE mv
SQL >

    SELECT
        customer_id,
        link_id,
        minState(timestamp) AS first_sale_state
    FROM dub_sale_events
    GROUP BY
        customer_id,
        link_id

TYPE materialized
DATASOURCE dub_first_sale_mv


