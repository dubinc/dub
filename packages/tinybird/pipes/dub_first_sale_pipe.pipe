NODE mv
SQL >

    SELECT
        workspace_id,
        link_id,
        customer_id,
        minState(timestamp) AS first_sale_state
    FROM dub_sale_events_mv
    GROUP BY
        workspace_id,
        link_id,
        customer_id

TYPE materialized
DATASOURCE dub_first_sale_mv


