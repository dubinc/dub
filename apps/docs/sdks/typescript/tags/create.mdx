---
title: "Create"
"og:title": "dub.tags.create - Create a new tag - TypeScript SDK"
description: "Create a new tag for the authenticated workspace."
---

import TagResponse from "/snippets/tag-response.mdx";

## Arguments

<ParamField body="tag" type="string" required>
  The name of the tag to create.
</ParamField>

<ParamField body="color" type="string">
  The color of the tag. If not provided, a random color will be used from the
  list: `red`, `yellow`, `green`, `blue`, `purple`, `pink`, `brown`.
</ParamField>

## Response

<TagResponse />

<RequestExample>

```ts
await dub.tags.create({
  tag: "news",
});
```

</RequestExample>

<ResponseExample>

```ts
{
  id: "clv3j1h7o00032u1ngo8luv84",
  name: "news",
  color: "blue"
}
```

</ResponseExample>
